@page
@model ApiDocumentationModel
@{
    ViewData["Title"] = "API Documentation";
}

<div class="container mt-4">
    <h1>API Documentation</h1>
    <p class="lead">This page documents the REST APIs available for external application communication.</p>

    <div class="row">
        <div class="col-md-12">
            <h2>Tax Authority API Endpoints</h2>
            <p>Base URL: <code>https://your-domain.com/api/UserProfileApi</code></p>

            <div class="card mb-4">
                <div class="card-header">
                    <h3>GET /api/UserProfileApi</h3>
                </div>
                <div class="card-body">
                    <p><strong>Description:</strong> Get all tax authorities with optional filtering</p>
                    <p><strong>Query Parameters:</strong></p>
                    <ul>
                        <li><code>clientCode</code> (optional): Filter by client code</li>
                        <li><code>authorityKey</code> (optional): Filter by authority key</li>
                    </ul>
                    <p><strong>Example:</strong></p>
                    <code>GET /api/UserProfileApi?clientCode=ABC&authorityKey=123</code>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h3>GET /api/UserProfileApi/{id}</h3>
                </div>
                <div class="card-body">
                    <p><strong>Description:</strong> Get a specific tax authority by ID</p>
                    <p><strong>Example:</strong></p>
                    <code>GET /api/UserProfileApi/1</code>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h3>POST /api/UserProfileApi</h3>
                </div>
                <div class="card-body">
                    <p><strong>Description:</strong> Create a new tax authority</p>
                    <p><strong>Request Body:</strong></p>
                    <pre><code>{
  "clientCode": "ABC123",
  "authorityKey": "AUTH001",
  "authorityName": "Sample Authority"
}</code></pre>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h3>PUT /api/UserProfileApi/{id}</h3>
                </div>
                <div class="card-body">
                    <p><strong>Description:</strong> Update an existing tax authority</p>
                    <p><strong>Example:</strong></p>
                    <code>PUT /api/UserProfileApi/1</code>
                    <p><strong>Request Body:</strong></p>
                    <pre><code>{
  "id": 1,
  "clientCode": "ABC123",
  "authorityKey": "AUTH001",
  "authorityName": "Updated Authority Name"
}</code></pre>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h3>DELETE /api/UserProfileApi/{id}</h3>
                </div>
                <div class="card-body">
                    <p><strong>Description:</strong> Delete a tax authority</p>
                    <p><strong>Example:</strong></p>
                    <code>DELETE /api/UserProfileApi/1</code>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-12">
            <h2>Testing the APIs</h2>
            <p>You can test these APIs using tools like:</p>
            <ul>
                <li>Postman</li>
                <li>curl</li>
                <li>Swagger UI (if you add Swagger to your project)</li>
            </ul>

            <h3>Example curl commands:</h3>
            <pre><code># Get all tax authorities
curl -X GET "https://your-domain.com/api/UserProfileApi"

# Get filtered results
curl -X GET "https://your-domain.com/api/UserProfileApi?clientCode=ABC"

# Create new tax authority
curl -X POST "https://your-domain.com/api/UserProfileApi" \
  -H "Content-Type: application/json" \
  -d '{"clientCode":"ABC123","authorityKey":"AUTH001","authorityName":"Test Authority"}'

# Update tax authority
curl -X PUT "https://your-domain.com/api/UserProfileApi/1" \
  -H "Content-Type: application/json" \
  -d '{"id":1,"clientCode":"ABC123","authorityKey":"AUTH001","authorityName":"Updated Authority"}'

# Delete tax authority
curl -X DELETE "https://your-domain.com/api/UserProfileApi/1"</code></pre>
        </div>
    </div>
</div> 